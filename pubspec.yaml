name: messenger
description: A new Flutter project.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=2.12.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  provider: ^5.0.0
  flutter_hooks: ^0.16.0
  cloud_firestore: ^1.0.0
  firebase_core: ^1.0.0
  firebase_auth: ^1.0.0
  shared_preferences: ^2.0.3
  contacts_service: ^0.5.0-nullsafety.0
  permission_handler: ^6.0.1
  mqtt_client: ^9.0.0
  uuid: ^3.0.1
  hive_flutter: ^1.0.0
  hive: ^2.0.4
  path_provider: ^2.0.1
  firebase_storage: ^8.0.0
  image_picker: ^0.7.2
  # flutter_image_compress: ^1.0.0-nullsafety
  cached_network_image: ^3.0.0-nullsafety
  intl: ^0.17.0
  connectivity: ^3.0.2
  indexed_list_view: ^2.0.0
  pointycastle: ^3.0.1
  rsa_encrypt:
    git:
      url: https://github.com/kannel-outis/rsa_encrypt.git
      ref: null-safety
  fluttertoast: ^8.0.6
  flutter_speed_dial: ^3.0.5
  timeago: ^3.0.2
  timeago_flutter: ^1.0.0
  flutter_isolate: ^2.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  hive_generator: null
  build_runner: null

flutter:

  uses-material-design: true

  assets:
    - flags/
    - assets/

